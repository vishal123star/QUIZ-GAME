{
  "name": "online_quiz",
  "version": "1.0.0",
  "description": "Online quiz",
  "dependencies": {},
  "devDependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json & live-server --ignorePattern=.*\\.json"
  },
  "author": "",
  "license": "ISC"
}
